@using BusinessSuite.Data
@using BusinessSuite.Models.Master_Models

@using Microsoft.AspNetCore.Identity
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager
@inject ApplicationDbContext _dbcontext
@{

    // Get the currently authenticated user
    var user = await UserManager.GetUserAsync(User);


    IList<string> roles = null;
    ApplicationUser applicationUser = null;
    // Check if the user is authenticated
    if (user != null)
    {

        applicationUser = _dbcontext.ApplicationUsers.Where(x => x.Id == user.Id).FirstOrDefault();
        // Get the user's roles
        roles = await UserManager.GetRolesAsync(user);

    }
    else
    {
    }
    var webId = 0;
    int? websiteId = TempData["WebsiteId"] as int?;

    // Check if count has a value and is greater than 0
    if (websiteId.HasValue)
    {
        webId = websiteId.HasValue ? websiteId.Value : 0;
    }
    var dbMaster = _dbcontext.DatabaseMasters.Where(i => i.WebsiteId == webId).FirstOrDefault();
    List<TableMaster> catalogs = null;
    if (dbMaster != null)
    {
        catalogs = _dbcontext.TableMasters.Where(i => i.DatabaseMasterId == dbMaster.Id).ToList();
    }

}



@if (SignInManager.IsSignedIn(User) && catalogs != null)
{
    <nav class="sidebar sidebar-offcanvas" id="sidebar">
        <ul class="nav">
            <li class="nav-item">
                <a class="nav-link" asp-controller="Home" asp-action="Index">
                    <i class="mdi mdi-grid-large menu-icon"></i>
                    <span class="menu-title">Dashboard</span>
                </a>
            </li>
            
            <li class="nav-item nav-category">Administration</li>
            <li class="nav-item">
                <a class="nav-link" data-bs-toggle="collapse" href="#ui-admin" aria-expanded="false" aria-controls="ui-admin">
                    <i class="menu-icon mdi mdi-account-multiple"></i>
                    <span class="menu-title">Users</span>
                    <i class="menu-arrow"></i>
                </a>
                <div class="collapse" id="ui-admin">
                    <ul class="nav flex-column sub-menu">
                        <li class="nav-item"> <a asp-controller="User" asp-action="GetUsers" class="nav-link">All Users</a></li>
                        <li class="nav-item"> <a asp-controller="User" asp-action="GetCompanies" class="nav-link">Companies</a></li>
                        <li class="nav-item"> <a class="nav-link" href="pages/ui-features/typography.html">Roles</a></li>
                    </ul>
                </div>
            </li>
             <!-- catalogue -->
            <li class="nav-item">
                <a asp-controller="Catalogue" asp-action="Index" asp-route-szDatabaseMasterId="@dbMaster.Id" class="nav-link">
                    <i class="menu-icon mdi mdi-folder-multiple-outline"></i>
                    <span class="menu-title">Catalogue</span>
                </a>
            </li>
            @* 
            <li class="nav-item">
                <a class="nav-link" data-bs-toggle="collapse" href="#ui-x" aria-expanded="false" aria-controls="ui-x">
                    <i class="menu-icon mdi mdi-account-multiple"></i>
                    <span class="menu-title">Catalogue</span>
                    <i class="menu-arrow"></i>
                </a>
                <div class="collapse" id="ui-x">
                    <ul class="nav flex-column sub-menu">
                        @foreach (var website in catalogs)
                        {
                            <li class="nav-item">
                                <a asp-controller="Catalogue" asp-action="DisplayTable"
                                   asp-route-szTableName="@website.Name"
                                   asp-route-szDatabaseMasterId="@website.DatabaseMasterId"
                                   class="nav-link">
                                    @website.Name
                                </a>
                            </li>
                        }
                    </ul>
                </div>
            </li>
        *@
        </ul>
       
    </nav>
    <!-- partial -->
   
}
